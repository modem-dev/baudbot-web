---
const features = [
  {
    emoji: "💬",
    title: "Slack is the UI",
    description: "No dashboards, no portals. Your team assigns work in Slack threads and gets progress updates in the same thread. Everyone sees what's happening."
  },
  {
    emoji: "⚡",
    title: "Always Running",
    description: "Baudbot is a persistent service, not a one-shot script. It's online at 2am, picks up tasks immediately, and never needs a poke to get started."
  },
  {
    emoji: "🔄",
    title: "Branch to PR, Handled",
    description: "It creates the branch, writes the code, pushes the PR, watches CI, and iterates on failures — without you touching a keyboard."
  },
  {
    emoji: "🐧",
    title: "Runs on Linux, Your Server",
    description: "Baudbot runs natively on Linux with full access to your actual toolchain — your compilers, your CLIs, your environment. No cloud sandbox, no vendor lock-in."
  },
  {
    emoji: "🧠",
    title: "Knows Your Codebase",
    description: "Baudbot builds up knowledge of your repo over time — the weird CI quirks, the internal conventions, the stuff that isn't in the README. It gets better the longer it runs."
  },
  {
    emoji: "🔧",
    title: "Self-Updating Skills",
    description: "When Baudbot figures out a better way to do something, it saves that. Future tasks benefit automatically. You don't have to write prompts or maintain playbooks."
  }
];
---

<section id="features" class="relative py-32">
  <!-- Section border -->
  <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-border-subtle to-transparent"></div>

  <div class="mx-auto max-w-6xl px-6">
    <!-- Section header -->
    <div class="mb-16">
      <div class="flex items-center gap-3 mb-4">
        <span class="text-accent font-mono font-bold text-lg">⟩</span>
        <span class="text-sm font-semibold text-accent uppercase tracking-widest">Capabilities</span>
      </div>
      <h2 class="text-4xl sm:text-5xl font-bold tracking-tight mb-4">
        Built for Teams
      </h2>
      <p class="text-lg text-text-secondary max-w-2xl">
        One agent deployment your whole engineering team shares. It runs on your server, talks through Slack, and keeps working when you're not.
      </p>
    </div>

    <!-- Feature grid with modem styling -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">
      {features.map((feature) => (
        <div class="group relative rounded-[3px] border border-border-subtle bg-bg-card p-7 hover:bg-bg-card-hover hover:border-accent/20 transition-all duration-300">
          <!-- Hover glow -->
          <div class="absolute inset-0 rounded-[3px] bg-accent/[0.02] opacity-0 group-hover:opacity-100 transition-opacity"></div>

          <div class="relative">
            <div class="text-3xl mb-4">{feature.emoji}</div>
            <h3 class="text-lg font-semibold mb-2 text-text-primary">
              {feature.title}
            </h3>
            <p class="text-sm text-text-secondary leading-relaxed">
              {feature.description}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
