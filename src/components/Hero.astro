---
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden pt-16">
  <!-- Gradient background effects -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-1/4 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[600px] bg-accent/[0.04] rounded-full blur-[120px]"></div>
    <div class="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-border-subtle to-transparent"></div>
  </div>

  <div class="relative mx-auto max-w-4xl px-6 py-24 text-center">
    <!-- Badge -->
    <div class="inline-flex items-center gap-2 rounded-full border border-accent/20 bg-accent/5 px-4 py-1.5 mb-8">
      <span class="relative flex h-2 w-2">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-accent opacity-75"></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-accent"></span>
      </span>
      <span class="text-sm font-medium text-accent">Open Source</span>
    </div>

    <!-- Logo -->
    <img
      src="/logo.png"
      alt="Baudbot logo"
      class="w-64 h-64 mx-auto mb-8"
      width="256"
      height="256"
    />

    <!-- Title -->
    <h1 class="text-6xl sm:text-7xl md:text-8xl font-black tracking-tight mb-6 leading-[0.9]">
      <span class="text-text-primary">Baud</span><span class="text-accent">bot</span>
    </h1>

    <!-- Tagline -->
    <p class="text-xl sm:text-2xl md:text-3xl font-semibold text-text-secondary mb-6 tracking-tight">
      A coding agent your whole team shares
    </p>

    <!-- Description -->
    <p class="text-base sm:text-lg text-text-secondary max-w-2xl mx-auto mb-10 leading-relaxed">
      Baudbot lives in Slack and runs on your Linux server. Message it a task, it opens a branch,
      writes the code, and ships a PR. Everyone on the team gets the same agent, already context-aware.
    </p>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
      <a
        href="https://github.com/modem-dev/baudbot"
        target="_blank"
        rel="noopener noreferrer"
        class="group inline-flex items-center gap-2 rounded-xl bg-accent px-8 py-3.5 text-base font-semibold text-bg-primary hover:bg-accent/90 transition-all shadow-lg shadow-accent/20 hover:shadow-accent/30"
      >
        View on GitHub
        <svg class="w-4 h-4 transition-transform group-hover:translate-x-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
        </svg>
      </a>
      <a
        href="https://app.baudbot.ai"
        target="_blank"
        rel="noopener noreferrer"
        class="group inline-flex items-center gap-2 rounded-xl border border-border-subtle bg-white/5 px-8 py-3.5 text-base font-semibold text-text-primary hover:bg-white/10 hover:border-accent/30 transition-all"
      >
        Get Started
        <svg class="w-4 h-4 transition-transform group-hover:translate-x-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
        </svg>
      </a>
    </div>

    <!-- Terminal preview hint -->
    <div class="mt-16 mx-auto max-w-xl">
      <div class="rounded-xl border border-border-subtle bg-terminal-bg/80 p-4 font-mono text-sm text-text-secondary backdrop-blur-sm">
        <div class="flex items-center gap-2 mb-3">
          <div class="w-3 h-3 rounded-full bg-red-500/60"></div>
          <div class="w-3 h-3 rounded-full bg-yellow-500/60"></div>
          <div class="w-3 h-3 rounded-full bg-green-500/60"></div>
        </div>
        <div>
          <span class="text-accent">$</span> baudbot deploy<br/>
          <span class="text-text-muted">▸ Connecting to Sentry... ✓</span><br/>
          <span class="text-text-muted">▸ Connecting to Slack... ✓</span><br/>
          <span class="text-text-muted">▸ Connecting to GitHub... ✓</span><br/>
          <span class="text-green-400">✓ Baudbot is ready for instructions from Slack</span>
        </div>
      </div>
    </div>
  </div>
</section>
